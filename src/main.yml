name: SCP Rust
settings:
    - SubcommandRequiredElseHelp
subcommands:
    - server:
        args:
            - headless:
                short: h
                long: headless
                help: Run server without prompt
                default_value: "false"
            - max_players:
                short: m
                long: max_players
                help: Maximum player slots for server
                default_value: "8"
    - client:
